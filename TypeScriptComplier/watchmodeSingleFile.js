"use strict";
console.log('setting up the app');
const userName = 'Angelo Shwebeth';
console.log("user name is : ", userName);
// as we need to recompile every time something changes
// in our code, we will set up "watch mode" which will automatically
// compile when we change our code : tsc app.ts --watch or -w
